%cite
  =author_link(comment)
says:
%br/
%small.commentmetadata
  %a{:href => "#comment-#{comment.id}", :title => ""}=format_comment_date(comment.created_at)
%p.gfm
  :preserve
    
    #{comment.body}
